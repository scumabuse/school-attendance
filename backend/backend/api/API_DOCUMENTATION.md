# API Документация

## Базовый URL
```
http://localhost:5000/api
```

## Аутентификация

Большинство маршрутов требуют JWT токен в заголовке:
```
Authorization: Bearer <token>
```

Токен получается при входе через `/api/auth/login` и действителен 8 часов.

---

## Маршруты

### 🔐 Аутентификация

#### POST `/api/auth/login`
Вход в систему

**Тело запроса:**
```json
{
  "login": "admin",
  "password": "admin123"
}
```

**Ответ:**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "uuid",
    "login": "admin",
    "role": "ADMIN",
    "fullName": "Администратор"
  }
}
```

#### POST `/api/auth/verify`
Проверка токена

**Заголовки:** `Authorization: Bearer <token>`

**Ответ:**
```json
{
  "valid": true,
  "user": {
    "id": "uuid",
    "login": "admin",
    "role": "ADMIN",
    "fullName": "Администратор"
  }
}
```

---

### 👥 Пользователи (требуется HEAD или ADMIN)

#### GET `/api/admin/users`
Получить список всех пользователей

**Ответ:**
```json
[
  {
    "id": "uuid",
    "login": "admin",
    "role": "ADMIN",
    "fullName": "Администратор",
    "createdAt": "2024-01-01T00:00:00.000Z"
  }
]
```

#### GET `/api/admin/users/:id`
Получить пользователя по ID

#### POST `/api/admin/users`
Создать нового пользователя

**Тело запроса:**
```json
{
  "login": "teacher1",
  "password": "password123",
  "role": "TEACHER",
  "fullName": "Иванов Иван Иванович"
}
```

#### PUT `/api/admin/users/:id`
Полное обновление пользователя

#### PATCH `/api/admin/users/:id`
Частичное обновление пользователя

#### DELETE `/api/admin/users/:id`
Удалить пользователя

---

### 📚 Специальности

#### GET `/api/specialties`
Получить список всех специальностей (доступно всем аутентифицированным)

**Ответ:**
```json
[
  {
    "id": 1,
    "code": "09.02.07",
    "name": "Информационные системы и программирование",
    "durationYears": 3,
    "qualifications": []
  }
]
```

#### GET `/api/admin/specialties/:id`
Получить специальность по ID

#### POST `/api/admin/specialties`
Создать новую специальность (требуется HEAD или ADMIN)

**Тело запроса:**
```json
{
  "code": "09.02.07",
  "name": "Информационные системы и программирование",
  "durationYears": 3
}
```

#### PUT `/api/admin/specialties/:id`
Полное обновление специальности

#### PATCH `/api/admin/specialties/:id`
Частичное обновление специальности

#### DELETE `/api/admin/specialties/:id`
Удалить специальность

---

### 🎓 Квалификации (требуется HEAD или ADMIN)

#### GET `/api/admin/qualifications`
Получить список всех квалификаций

#### GET `/api/admin/qualifications/:id`
Получить квалификацию по ID

#### POST `/api/admin/qualifications`
Создать новую квалификацию

**Тело запроса:**
```json
{
  "name": "Программист",
  "specialtyCode": "09.02.07",
  "type": "IT_TECHNICIAN"
}
```

#### PUT `/api/admin/qualifications/:id`
Полное обновление квалификации

#### PATCH `/api/admin/qualifications/:id`
Частичное обновление квалификации

#### DELETE `/api/admin/qualifications/:id`
Удалить квалификацию

---

### 🎉 Праздники (требуется HEAD или ADMIN)

#### GET `/api/admin/holidays`
Получить список всех праздников

**Ответ:**
```json
[
  {
    "id": 1,
    "date": "2024-01-01T00:00:00.000Z",
    "name": "Новый год"
  }
]
```

#### GET `/api/admin/holidays/:id`
Получить праздник по ID

#### POST `/api/admin/holidays`
Создать новый праздник

**Тело запроса:**
```json
{
  "date": "2024-01-01",
  "name": "Новый год"
}
```

#### PUT `/api/admin/holidays/:id`
Полное обновление праздника

#### PATCH `/api/admin/holidays/:id`
Частичное обновление праздника

#### DELETE `/api/admin/holidays/:id`
Удалить праздник

---

### 👨‍👩‍👧‍👦 Группы (требуется HEAD или ADMIN)

#### GET `/api/admin/curators`
Получить список кураторов (для дропдауна)

**Ответ:**
```json
[
  {
    "id": "uuid",
    "fullName": "Иванов Иван Иванович",
    "role": "TEACHER"
  }
]
```

#### GET `/api/admin/groups`
Получить список всех групп

**Ответ:**
```json
[
  {
    "id": "uuid",
    "name": "ИСП-21",
    "admissionYear": 2021,
    "course": 3,
    "currentCourse": 3,
    "specialtyId": 1,
    "specialty": {
      "code": "09.02.07",
      "name": "Информационные системы и программирование"
    },
    "curatorId": "uuid",
    "curator": {
      "id": "uuid",
      "fullName": "Иванов Иван Иванович"
    },
    "createdAt": "2024-01-01T00:00:00.000Z",
    "updatedAt": "2024-01-01T00:00:00.000Z"
  }
]
```

#### GET `/api/admin/groups/:id`
Получить группу по ID

#### POST `/api/admin/groups`
Создать новую группу

**Тело запроса:**
```json
{
  "name": "ИСП-21",
  "specialtyCode": "09.02.07",
  "course": 3,
  "admissionYear": 2021,
  "curatorId": "uuid"
}
```

#### PUT `/api/admin/groups/:id`
Полное обновление группы

#### PATCH `/api/admin/groups/:id`
Частичное обновление группы

#### DELETE `/api/admin/groups/:id`
Удалить группу

---

### 🎓 Студенты

#### GET `/api/students`
Получить список всех студентов (доступно всем аутентифицированным)

**Query параметры:**
- `groupId` (опционально) - фильтр по группе

**Ответ:**
```json
[
  {
    "id": "uuid",
    "fullName": "Петров Петр Петрович",
    "groupId": "uuid",
    "group": {
      "id": "uuid",
      "name": "ИСП-21",
      "specialty": {
        "id": 1,
        "code": "09.02.07",
        "name": "Информационные системы и программирование"
      }
    },
    "createdAt": "2024-01-01T00:00:00.000Z"
  }
]
```

#### GET `/api/students/:id`
Получить студента по ID

#### POST `/api/admin/students`
Создать нового студента (требуется HEAD или ADMIN)

**Тело запроса:**
```json
{
  "fullName": "Петров Петр Петрович",
  "groupId": "uuid"
}
```

#### PUT `/api/admin/students/:id`
Полное обновление студента

#### PATCH `/api/admin/students/:id`
Частичное обновление студента

#### DELETE `/api/admin/students/:id`
Удалить студента

#### POST `/api/admin/import/students`
Импорт студентов из Excel файла (требуется HEAD или ADMIN)

**Тело запроса:** FormData с полем `file` (Excel файл)

**Ответ:**
```json
{
  "message": "Успешно импортировано: 25 студентов",
  "totalInFile": 30,
  "importedCount": 25,
  "skippedCount": 3,
  "errorsCount": 2,
  "errors": ["Строка 5: ГРУППА НЕ НАЙДЕНА → \"ИСП-22\""]
}
```

---

### ✅ Посещаемость (требуется аутентификация)

#### GET `/api/attendance/log`
Получить лог посещаемости

**Query параметры:**
- `groupId` (опционально) - фильтр по группе
- `date` (опционально) - фильтр по дате (ISO date)
- `studentId` (опционально) - фильтр по студенту
- `startDate` (опционально) - начало периода (ISO date)
- `endDate` (опционально) - конец периода (ISO date)

**Ответ:**
```json
[
  {
    "id": "uuid",
    "studentId": "uuid",
    "groupId": "uuid",
    "fullName": "Петров Петр Петрович",
    "groupName": "ИСП-21",
    "status": "PRESENT",
    "date": "2024-01-15T00:00:00.000Z",
    "updatedAt": "2024-01-15T10:30:00.000Z",
    "updatedBy": "Иванов Иван Иванович"
  }
]
```

#### GET `/api/attendance`
Получить посещаемость с фильтрами

#### GET `/api/attendance/:id`
Получить запись посещаемости по ID

#### POST `/api/attendance`
Создать или обновить запись посещаемости

**Тело запроса:**
```json
{
  "studentId": "uuid",
  "groupId": "uuid",
  "date": "2024-01-15",
  "status": "PRESENT"
}
```

**Статусы посещаемости:**
- `PRESENT` - Присутствовал
- `ABSENT` - Отсутствовал
- `SICK` - Болел
- `ITHUB` - IT Hub
- `VALID_ABSENT` - Уважительная причина

#### PUT `/api/attendance/:id`
Полное обновление записи посещаемости

#### PATCH `/api/attendance/:id`
Частичное обновление записи посещаемости

#### DELETE `/api/attendance/:id`
Удалить запись посещаемости

#### POST `/api/attendance/batch`
Массовое создание/обновление посещаемости

**Тело запроса:**
```json
{
  "records": [
    {
      "studentId": "uuid",
      "groupId": "uuid",
      "date": "2024-01-15",
      "status": "PRESENT"
    }
  ]
}
```

**Ответ:**
```json
{
  "created": 10,
  "updated": 5,
  "errors": []
}
```

---

### 🔧 Административные маршруты (требуется HEAD или ADMIN)

#### GET `/api/export/attendance`
Экспорт посещаемости в Excel

**Query параметры:**
- `groupIds` (опционально) - ID групп через запятую (например "id1,id2,id3")
- `dateRangeType` (опционально) - тип периода: `today`, `week`, `month`, `academic_year`, `custom`
- `startDate` (опционально) - начало периода для `custom` (ISO date)
- `endDate` (опционально) - конец периода для `custom` (ISO date)

**Ответ:** Excel файл

#### POST `/api/admin/finish-year`
Завершение учебного года

**Ответ:**
```json
{
  "success": true,
  "message": "Год завершён. Архивировано в attendance_archive_2023_2024",
  "deletedGroups": ["ИСП-18", "ИСП-19"]
}
```

#### GET `/api/admin/stats`
Получить статистику системы

**Ответ:**
```json
{
  "users": 10,
  "groups": 15,
  "students": 300,
  "specialties": 5,
  "attendanceRecords": 5000
}
```

#### GET `/api/admin/stats/attendance`
Получить статистику посещаемости

**Query параметры:**
- `groupId` (опционально) - фильтр по группе
- `startDate` (опционально) - начало периода (ISO date)
- `endDate` (опционально) - конец периода (ISO date)

**Ответ:**
```json
{
  "total": 100,
  "present": 80,
  "absent": 15,
  "sick": 3,
  "validAbsent": 2,
  "ithub": 0,
  "percentage": "84.21"
}
```

#### GET `/api/test`
Тестовый маршрут для проверки работы сервера (без аутентификации)

**Ответ:**
```json
{
  "message": "Бэкенд работает!"
}
```

---

## Коды ошибок

- `400` - Неверный запрос (неполные данные, валидация)
- `401` - Не авторизован (отсутствует или неверный токен)
- `403` - Доступ запрещён (недостаточно прав)
- `404` - Ресурс не найден
- `500` - Ошибка сервера

---

## Роли пользователей

- `ADMIN` - Администратор (полный доступ)
- `HEAD` - Заведующий (доступ к админ-панели)
- `TEACHER` - Преподаватель (ограниченный доступ)

---

## Примечания

1. Все даты должны быть в формате ISO (например: `2024-01-15`)
2. UUID используются для идентификаторов пользователей, групп, студентов и записей посещаемости
3. Числовые ID используются для специальностей, квалификаций и праздников
4. При массовых операциях возможны частичные ошибки (код ответа 207)
5. Экспорт посещаемости возвращает Excel файл, а не JSON




